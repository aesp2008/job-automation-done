# infrastructure

This directory will contain Dockerfiles, docker-compose configuration,
and environment templates for local development and deployment.

Concrete files like `docker-compose.yml`, `Dockerfile.backend`,
`Dockerfile.frontend`, and `env.example` will be added in a later milestone.

