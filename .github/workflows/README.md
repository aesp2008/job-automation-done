# GitHub Workflows

This directory will hold CI workflows (e.g., `ci.yml`) for linting,
testing, and other automation on pushes and pull requests.

The initial CI workflow will be added in a later milestone.

